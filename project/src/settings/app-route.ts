enum AppRoute {
  Favorites = '/favorites',
  FavoritesEmpty = '/favorites-empty',
  Login = '/login',
  Logout = '/logout',
  NotFound = '/404',
  Property = '/offer/:id',
  Root = '/',
  City = '/:cityCode',
}

export {AppRoute};
